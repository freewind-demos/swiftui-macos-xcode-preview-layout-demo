name: SwiftUIXcodePreviewLayoutDemo
options:
  bundleIdPrefix: com.freewind
  deploymentTarget:
    macOS: "14.0"
settings:
  base:
    SWIFT_VERSION: "5.9"
    MACOSX_DEPLOYMENT_TARGET: "14.0"
targets:
  SwiftUIXcodePreviewLayoutDemo:
    type: application
    platform: macOS
    sources:
      - Sources
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.freewind.SwiftUIXcodePreviewLayoutDemo
        INFOPLIST_FILE: Sources/Info.plist
        CODE_SIGN_IDENTITY: "-"
        CODE_SIGN_STYLE: Manual
        DEVELOPMENT_TEAM: ""
        ENABLE_HARDENED_RUNTIME: NO
    productName: SwiftUIXcodePreviewLayoutDemo
schemes:
  SwiftUIXcodePreviewLayoutDemo:
    build:
      targets:
        SwiftUIXcodePreviewLayoutDemo: all
    run:
      config: Debug
